[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "normos-lab-demo"
version = "0.1.0"
dependencies = [
    "pydantic",
    "pandas",
    "spacy",
    "pymorphy3",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["core*", "packages*"]